# 2026-02-19 — UI Fixes (Light Mode + Loading Spinner)

## Changes Made

### 1. Pledge Modal Checkbox (Light Mode Fix)
- **File:** `src/lib/components/pledge/PledgeForm.svelte`
- Native checkbox looked too dark in light mode
- Replaced with custom-styled checkbox using `appearance: none`, CSS vars (`--color-input` bg, `--color-border` border), SVG checkmark on checked state
- Added `<style>` block with `.pledge-checkbox` class

### 2. LoadingSpinner Visibility on Primary Buttons
- **File:** `src/lib/components/shared/LoadingSpinner.svelte`
- Changed from `border-primary` to `border-current` so spinner inherits parent text color
- On `bg-primary` buttons, now uses `text-primary-foreground` (visible) instead of same-as-background
- Added optional `class` prop (backward compatible)

### 3. Outline Button Variant (Light Mode Fix)
- **File:** `src/lib/components/ui/button/button.svelte`
- Changed light-mode bg from `bg-background` (#edeee8 grayish) to `bg-input` (#ffffff white)
- Made `border-border` explicit so light mode gets white bg with darker border
- Dark mode unchanged (`dark:bg-input/30`)

## Test Results
- 0 TypeScript errors, 682 tests passing
- All changes backward compatible
