# 2026-02-20

## Ninja Logo Loading Transition Enhancement
- Added "ninja moves to navbar" effect on initial page load
- Flow: center ninja pulses → fades out (300ms) → 1s pause → navbar logo fades in (1s CSS transition)
- Implementation:
  - `page-loading.svelte.ts`: added `navLogoVisible` signal + `showNavLogo(delayMs)` method
  - `Header.svelte`: CSS classes `nav-logo-hidden`/`nav-logo-visible` driven by `pageLoading.navLogoVisible`
  - All 3 pages with loading overlays (`+page.svelte`, `bounty/[naddr]`, `profile/[npub]`) call `pageLoading.showNavLogo(1000)` via `onoutroend`
  - Respects `prefers-reduced-motion`
- Only triggers on initial loads when data is being fetched (cached data skips overlay entirely)
